name: test_websocket
description: A web server built using the shelf package.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  socket_io_client:
dev_dependencies:
  pedantic: ^1.9.0
